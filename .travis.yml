language: python
python:
  - "3.5"
# command to install dependencies
install:
  - pip install iso8601
  - pip install pynacl cryptography wolframalpha wikipedia oauth2client cleverbot gspread
  - pip install -r requirements.txt
# command to run tests
script: python3.5 run.py
