language: python
python:
  - "3.5"
# command to install dependencies
install:
  - pip install iso8601
  - pip install pynacl cryptography wikipedia wolframalpha oauth2client cleverbot gspread
  - pip install -r requirements.txt
  - sudo apt-get install pylint
# command to run tests
script: pylint rtb/bot.py
