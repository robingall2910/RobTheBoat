language: python
python:
  - "3.4"
  - "3.5"
# command to install dependencies
install:
  - pip install pynacl cryptography wolframalpha apiai
  - pip install -r requirements.txt
# command to run tests
script: python3.5 run.py
